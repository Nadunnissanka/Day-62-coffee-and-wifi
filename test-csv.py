# import pandas as pd
#
# data = pd.read_csv("cafe-data.csv")
# list_of_rows = []
# # loop through each row and record specific data
# for index, row in data.iterrows():
#     each_row = (row['Cafe Name'], row['Location'], row['Open'], row['Close'], row['Coffee'], row['Wifi'], row['Power'])
#     list_of_rows.append(each_row)
# print(list_of_rows)

# test code for csv data issue ps: full path will resolve
